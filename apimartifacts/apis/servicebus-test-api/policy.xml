<!--
    - Policies are applied in the order they appear.
    - Position <base/> inside a section to inherit policies from the outer scope.
    - Comments within policies are not preserved.
-->
<!-- Add policies as children to the <inbound>, <outbound>, <backend>, and <on-error> elements -->
<policies>
	<!-- Throttle, authorize, validate, cache, or transform the requests -->
	<inbound>
		<base />
		<set-variable name="requestBody" value="@((string)context.Request.Body.As<string>(true))" />
		<!--<send-request mode="new" response-variable-name="sbResponse">
            <set-url>https://servicebus01.servicebus.windows.net/test-queue/messages</set-url>
            <set-method>POST</set-method>
            <authentication-managed-identity resource="https://servicebus.azure.net/" />
            <proxy>
                <set-header name="Content-Type" exists-action="override">
                    <value>application/json; charset=utf-8</value>
                </set-header>
            </proxy>
            <set-body-template>@(context.Request.Body.As<string>(true))</set-body-template>
        </send-request>-->
		<!-- Working code -->
		<!--<send-request mode="new" response-variable-name="serviceBusResponse" timeout="20" ignore-error="false">
            <set-url>https://servicebus01.servicebus.windows.net/test-queue/messages</set-url>
            <set-method>POST</set-method>
            <set-body>@{
        return context.Request.Body.As<string>(preserveContent: true);
    }</set-body>
            <authentication-managed-identity resource="https://servicebus.azure.net/" />
        </send-request>-->
		<authentication-managed-identity resource="https://servicebus.azure.net/" client-id="86b7bf6f-f716-4297-8c12-3da8577d4006" />
		<set-backend-service base-url="https://servicebus01.servicebus.windows.net/test-queue/messages" />
	</inbound>
	<!-- Control if and how the requests are forwarded to services  -->
	<backend>
		<base />
	</backend>
	<!-- Customize the responses -->
	<outbound>
		<base />
	</outbound>
	<!-- Handle exceptions and customize error responses  -->
	<on-error>
		<base />
	</on-error>
</policies>